import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nieva.cargacredito',
  appName: 'Carga De Credito',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
